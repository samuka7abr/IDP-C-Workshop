#include <stdio.h>

void contaFrequencia(/* parâmetros */) {
    // corpo da função
}

int main() {
    int n;
    // leitura de n
    // declaração do array
    // leitura dos elementos
    contaFrequencia(/* argumentos */);
    return 0;
}
